# `0x83000000` UI 与世界 Catalog

本目录承载 UI、地图和场景 catalog 的目录语义。

目录级语义由本 README 承载，独立专题文件只承载具体问题、链路或清单。

## `0x83001000` 目录职责

### `0x83001100` 文档边界

#### `0x83001110` 目录载体

##### `0x83001111` 验证点

本 README 只说明目录定位、子页面边界和推荐入口。

具体源码行为进入本目录下的独立专题文件。

不要让第一个独立文件替代目录 README。

## `0x83002000` 子页面索引

### `0x83002100` 推荐顺序

#### `0x83002110` 从目录到专题

##### `0x83002111` 链接校验

- [UI Catalog](0x8301-ui-catalog.md)
- [Map Scenario Catalog](0x8302-map-scenario-catalog.md)

## `0x83003000` 阅读入口

### `0x83003100` 最小路径

#### `0x83003110` 先定位再展开

##### `0x83003111` 抽样动作

优先进入 `0x8301-ui-catalog.md`，再按页面索引顺序展开。

如果要查完整清单，回到 `0x8000-reference/README.md`。
